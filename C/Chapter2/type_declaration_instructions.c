#include<stdio.h>

int main() {
    int a = 20; 
    int b = a; 
    int c = b * 3; 
    int d = 2, e;

    int oldAge = 20; 
    int years = 2; 
    int newAge = oldAge + years; 

    int x, y, z; 
    int x = y = z = 4; 

    return 0;
}